<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">

        <activity android:name=".authentication.RegisterActivity"
            android:exported="true">

        </activity>
         <activity
         android:name=".authentication.LoginActivity"
         android:exported="false">
         </activity>
        <activity
            android:name=".Developer"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Website"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ebook.PdfViewerActivity"
            android:exported="false" />
        <activity
            android:name=".ebook.EbookActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".paper.PaperViewerActivity"
            android:exported="false" />
        <activity
            android:name=".paper.PaperActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Results"
            android:parentActivityName=".MainActivity" /> <!-- Add FullImageView activity here -->
        <activity
            android:name=".FullImageView"
            android:exported="false">

            <!-- Add any necessary intent filters here if needed -->
        </activity>
        <activity
            android:name=".SplashScreen"
            android:exported="true"
            android:theme="@style/SplashScreen">

        </activity>
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
<!--    android:theme="@style/Theme.GHRCEUSER" />-->

</manifest>